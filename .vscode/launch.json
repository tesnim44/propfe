{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch via Live Server",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost/iblog/index.php",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
